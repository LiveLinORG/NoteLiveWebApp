import axios from 'axios';

const BASE_URL = 'http://190.239.59.223:44353';
/*
export default {
    async signIn(signInData) {
        try {
            const response = await axios.post(`${BASE_URL}/sign-in`, signInData);
            return response.data;
        } catch (error) {
            throw error.response.data;
        }
    },

    async signUp(signUpData) {
        try {
            const response = await axios.post(`${BASE_URL}/sign-up`, signUpData);
            return response.data;
        } catch (error) {
            throw error.response.data;
        }
    }
}
*/

